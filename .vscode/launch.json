{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/google_agents/fetch_address.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}